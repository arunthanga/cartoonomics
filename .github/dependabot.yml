version: 2
updates:
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: weekly
    groups:
      python-dev:
        patterns: ["*"]

  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly

  # Enable once the frontend package.json lands.
  # - package-ecosystem: npm
  #   directory: "/apps/web"
  #   schedule:
  #     interval: weekly
