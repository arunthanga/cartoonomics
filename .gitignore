# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.hypothesis/
build/
dist/

# Local TDD-mode toggle state (default is ON in code)
.tdd-mode

# Virtual envs
.venv/
venv/

# Node / frontend (apps/web when the React app lands)
node_modules/
dist/
.vite/
*.tsbuildinfo

# Local env / secrets (never commit — TA-4)
.env
.env.*
!.env.example

# Scraped source data (raw + parsed) — never commit vendor data (CMP-3)
sebi_pms_data/
